# server ui warper

it just help mainly on modal form, becouse modal for some reason still add label and other stuff the its output.

to use it is just like normal server ui form
[typescript](../ts/srv-ui.ts) and [javascript](../js/srv-ui.js)
